# --- Web / API ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20
websockets==14.1

# --- DB / ORM ---
sqlalchemy==2.0.36
alembic==1.14.0

# --- Validation / settings ---
pydantic==2.10.4
pydantic-settings==2.7.0

# --- AI ---
anthropic==0.42.0
# google-genai>=1.0.0   # ставится отдельно, если LLM_PROVIDER=gemini

# --- HTTP ---
httpx==0.28.1

# --- Niche analysis (Google Trends, опционально) ---
# Без него анализ ниш работает на YouTube Data API + LLM, но без динамики Trends.
pytrends==4.9.2

# --- Logging ---
loguru==0.7.3

# --- Tests ---
pytest==8.3.4
pytest-asyncio==0.25.0

# --- Optional / heavy (TTS, video) — устанавливаются отдельно при необходимости ---
# kokoro>=0.9.2
# soundfile==0.13.0
# moviepy==2.1.1
# faster-whisper==1.1.0
# pillow==11.1.0
# replicate>=1.0.0   # AI-визуальный ряд (FLUX), нужен REPLICATE_API_TOKEN в .env
